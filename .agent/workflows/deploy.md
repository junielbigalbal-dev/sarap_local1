---
description: Push changes to GitHub to trigger Render deployment
---

1. Check git status
2. Add all changes
   ```powershell
   git add .
   ```
3. Commit changes
   ```powershell
   git commit -m "Update application code"
   ```
4. Push to remote
   ```powershell
   git push origin main
   ```
